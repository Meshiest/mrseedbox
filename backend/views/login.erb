<!DOCTYPE html>
<html>
  <head>
    <title>Greeting</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <meta name="theme-color" content="#00aaee" />
    <style>
      * {
        margin: 0;
        padding: 0;
      }

      body {
        align-items: center;
        background: #f5f5f5;
        display: flex;
        height: 100vh;
        justify-content: center;
        overflow: hidden;
        width: 100vw;
      }

      .signin-button {
        align-items: center;
        border: none;
        border-radius: 50%;
        cursor: pointer;
        display: flex;
        height: 20vh;
        justify-content: center;
        transition: all 0.2s;
        width: 20vh;
      }

      .signin-button:hover {
        background: #009688;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
      }

      .signin-button:hover .unlocked,
      .signin-button:not(:hover) .locked {
        display: none;
      }

      .signin-text i {
        color: #999;
        font-size: 10vh;
      }

      .signin-button:hover i {
        color: #fff;
      }
    </style>
  </head>
  <body>
    <div class="signin-button" onclick="location.href='/auth'">
      <div class="signin-text">
        <i class="material-icons locked">lock_open</i>
        <i class="material-icons unlocked">lock_outline</i>
      </div>
    </div>
  </body>
</html>